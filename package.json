{
  "name": "personalabs",
  "version": "0.1.0",
  "private": true,
  "description": "Intentional semantic navigation layer for YouTube exploration.",
  "scripts": {
    "check": "node --check src/semantic-core.js && node --check src/content.js",
    "test": "node --test"
  },
  "devDependencies": {}
}
